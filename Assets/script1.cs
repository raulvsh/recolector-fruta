using UnityEngine;

public class script1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        gameObject.transform.Translate(-3,-3,-3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
